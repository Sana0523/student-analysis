/**
 * Components Barrel Export
 * ========================
 * Central export point for all reusable UI components.
 * 
 * Usage:
 *   import { Modal, StudentTable, GradeTable, PredictionCard } from '@/components';
 */

export { default as Modal } from './Modal';
export { default as StudentTable } from './StudentTable';
export { default as GradeTable } from './GradeTable';
export { default as PredictionCard } from './PredictionCard';
